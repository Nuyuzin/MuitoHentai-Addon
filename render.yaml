services:
  - type: web
    name: muito-hentai-stremio-addon
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /manifest.json
    envVars:
      - key: NODE_VERSION
        value: 20
